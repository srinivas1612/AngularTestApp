1. get user details and save
   -- change response handling in API and .ts file

2. Change pwd
     -- change response handling in API and .ts file

3. loading
     -- Change image, keep image locally.

4. Pwd encrypt and decrypt
   https://stackoverflow.com/questions/10168240/encrypting-decrypting-a-string-in-c-sharp

5. Take comments and rating and store in one table.
   after inserting calculate rating and store in main table

    decimal ratingSum = sum of rating;
    decimal ratingCount = count of ratings;

    decimal rating = 0;
    if (ratingCount > 0)
    {
        rating = (ratingSum / ratingCount);
    }
    
----------------------