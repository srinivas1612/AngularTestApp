
startup.cs:
------------

public Startup(IConfiguration configuration, IHostingEnvironment env)
        {
            var builder = new ConfigurationBuilder()
              .SetBasePath(env.ContentRootPath)
              .AddJsonFile("appsettings.json", optional: true, reloadOnChange: true)
              .AddJsonFile($"appsettings.{env.EnvironmentName}.json", optional: true)
              .AddEnvironmentVariables();

            Configuration = builder.Build();
        }
        
        in ConfigureServices method:
        
           services.Configure<Configurations>(Configuration.GetSection("Configurations"));

create manully production appsettings file and add configuration settings in dev and prod files

"Configurations": {
    "WebsiteURL": "production url"
  }
  
   public class Configurations
    {
        public string WebsiteURL { get; set; }
    }
    
     private readonly Configurations _configurationSettings;

        public ValuesController(IOptions<Configurations> configurationSettings)
        {
            _configurationSettings = configurationSettings.Value;
        }
        
for more info:

https://blog.elmah.io/config-transformations-in-aspnetcore/


send mail template:
https://www.tonyranieri.com/blog/2018/01/31/Sending-HTML-Templated-Emails-From-.NET-Core-2.0/

create folder like MailTemplates and add files

AccountActivationTemplate:
---------------------------
Hello {username},
<br/><br/>
You have been registered successfully on Courseazy.com!!

<br/><br/>
<b>Please click the link below to activate your account:</b>
<br/><br/>
{linkhere}
</br/><br/><br/>
<b>Best regards,</b><br/>
Team Courseazy


